/*fuction forEach(array,callback) {
     for (var i = 0; i < array.length; i++)
       callback(array[i]);
       I really hope this is fixed now. This is just text so that I have something new to commit 
*/